# TBD in design-first
# Define endpoints and contracts after UX_MAP and MODEL alignment.

# TODO (FP1 scope):
# Public
# - GET /games?tag=...&sort=...
# - GET /games/{id}
# - GET /tags (optional; derived from games tags_user/tags_system)
#
# Admin
# - POST /admin/teams
# - PATCH /admin/teams/{id}
# - POST /admin/games
# - PATCH /admin/games/{id}
# - POST /admin/games/{id}/build (upload ZIP)
# - POST /admin/games/{id}/publish (or /status)
# - POST /admin/games/{id}/status (editing/published/archived + remark)
# - POST/PATCH /admin/games/{id}/tags (restricted; tags_user/tags_system)
#
# Constraints
# - Publish requires cover_url + description_md + build_url/build_id.
# - Tag assignment allowed for team members + Super Admin (FP1: Super Admin only).
# - Max upload size 300 MB (configurable later).
# - Build delivery uses signed build_url for iframe (or equivalent secure URL).
